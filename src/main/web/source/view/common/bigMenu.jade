.bigMenu
    .menu
        span(class="glyphicon glyphicon-cloud" aria-hidden="true")    十度网盘
        .menuItem(ng-repeat="menu in bigMenus|roleFilter")
            span(ng-bind="menu.data.name" ng-click="jump(menu)")
    .content(ui-view="")

